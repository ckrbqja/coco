## 1. 본문 (차트 분석 및 매매 기법)

### 기본 원칙 및 추세 판단
- 10/12 1분봉 기준 매매
- 우상향일때가 상승이 크니까 가급적 우상향일때 매매
- ★ 항상 우상향 일때 매수 50선, 100선
- 100, 120선이 아래로 흐를때는 하락추세, 매수 금지
- 전고점, 전저점 수평선은 필히 긋는다 (매매참조)
- ★ 하락 저점이 올라가면 상승이다
- 20선 아래 50, 100선이 지지해주면 계속 전진한다

### 진입 (매수) 조건
- 20선이 바닥에서 우상향으로 향할때 준비
- 20선과 50선이 가까이 있을때는 50선 매수도 된다
- 상승중 저항이 있고 지지를 딛고 떨어지면 매도후 저점에 매수 한다
- 1차상승 눌림은 전고점 지지받을때
- ★ 5일선은 20, 50선 아래에서 돌파할때 매수
- ★ 하락추세에서 20선이 50선 돌파 매수 동참 (상승으로 전환)
- ★ 하락장에서 50선 캔들 안착 매수후 20선이 뒤따라 50선을 돌파 우상향하면 20선 따라 [판독불가]

### 매도 (익절/손절) 조건
- 매도 : 볼린저 상단 돌파하고 안으로 캔들이 들어올때 / 하락추세 손절후 저항이 나올때
- {추정: 매도조건은} 20선이 100선 {추정: 하락갈때 | 하향할때}

### 09 매매법 (장 시작 매매)
- 1. 09시에 상승 하락하면 전저점 매수
- 2. 09시에 하락하고 상승할때 50선 안착 매수
- 3. 09 상승으로 시작하면 50선 매수

### 이평선 수렴 및 볼린저 밴드 매매
- 이평 수렴 20, 50, 100선 50선 돌파매수
- 볼린저 쌍바닥, 뚫고 매매
- 볼린저 쌍바닥, 뚫고 3바닥 매수

## 2. AI 답변 및 단계별 시각화 가이드 (Response & Rendering Guide)

(1) 기본 답변 방식
- **답변 순서 템플릿**:
  - (a) 시장상태/추세 요약 (우상향/하락추세 여부 판단)
  - (b) 핵심 근거 (20선, 50선, 100선, 전고점/전저점, 볼린저 밴드 등 노트 기재 요소)
  - (c) 진입 타점 (전저점 매수, 50선 안착 매수, 볼린저 쌍바닥 뚫고 매수 등)
  - (d) 무효화 조건 (100, 120선이 아래로 흐를 때 등)
  - (e) 손절 (하락추세 손절후 저항 발생 시 등)
  - (f) 1차/2차 익절 (볼린저 상단 돌파 후 안으로 들어올 때 등)
  - (g) 리스크/주의사항 (전고점/전저점 수평선 필히 그을 것 등)
- **표현 원칙**:
  - 1분봉, 20선, 50선, 100선, 120선, 볼린저 상단/쌍바닥 등 원문의 용어를 그대로 사용한다.
  - 사용자가 묻지 않은 보조지표(RSI, MACD 등)는 임의로 추가하여 설명하지 않는다.

(2) 1단계 (빠른 시각화): ASCII 그래프 규칙
- 사용자가 일반적인 설명을 요청할 경우 코드 블록을 활용하여 ASCII 차트를 그린다.
- **표기 기호 매핑**:
  - `■` / `□` : 캔들 (상승/하락)
  - `~` : 20선
  - `=` : 50선
  - `-` : 100선 / 120선
  - `*` : 타점 (매수/매도)
  - `---` : 전고점/전저점 수평선 및 볼린저 밴드 경계
- **예시 ASCII 그래프** (하락추세에서 20선이 50선 돌파 매수 동참 패턴):
```text
[하락추세 -> 상승전환: 20선이 50선 돌파 매수]

Price
  |                                        □ 캔들안착
  |         = (50선) = = = = = = = = = = ■ = = = = = =
  |      =                               ~ (20선 돌파)
  |   =                            ~   * 매수동참
  |~ (20선)                  ~  ■
  |     ~                 ~  □
  |        ~     바닥   ~
  |          ~   □ ■  ~
  |             ~  ~
  +-------------------------------------------------- Time
```

(3) 2단계 (정밀 시각화): 파이썬 렌더링 또는 외부 실제 차트 첨부
- 사용자가 "자세히 그려줘", "정확한 차트를 보여줘"라고 요청할 시 아래 두 방법 중 하나를 제공한다.
- **A) 파이썬 렌더링 (가상의 캔들 차트)**:
  - **캔들 타입**: `candle`
  - **봉 개수**: default 80봉
  - **거래량 표시 여부**: 노트에 명시되지 않았으므로 default `False`
  - **이평선 기간**: 노트에 기재된 `20`, `50`, `100`, `120` 사용 (`mav=(20, 50, 100)`)
  - **지지/저항선**: 수평선 기능(`hlines`)을 통해 `전고점`/`전저점` 라인 표시
  - **볼린저 밴드**: 명시된 `볼린저 상단`, `볼린저 쌍바닥` 표현을 위해 상/하단 밴드 렌더링 추가
  - **마커**: `안착 매수`, `볼린저 상단 매도` 위치에 `^`, `v` 마커 추가
- **파이썬 렌더링 코드 예시**:
```python
import pandas as pd
import numpy as np
import mplfinance as mpf

# 1. 가상의 1분봉 데이터 생성 (하락 후 50선 안착 및 20선 돌파 우상향 시나리오)
dates = pd.date_range('2023-10-12 09:00', periods=80, freq='1T')
np.random.seed(42)
data = np.cumsum(np.random.randn(80) * 2) + 100
# 캔들 구성을 위한 OHLC 데이터 조작
df = pd.DataFrame({
    'Open': data + np.random.randn(80),
    'High': data + np.random.rand(80) * 3,
    'Low': data - np.random.rand(80) * 3,
    'Close': data
}, index=dates)

# 볼린저 밴드 및 이평선 계산 (20선, 50선, 100선)
df['MA20'] = df['Close'].rolling(window=20).mean()
df['MA50'] = df['Close'].rolling(window=50).mean()
df['MA100'] = df['Close'].rolling(window=100).mean()
df['BB_Upper'] = df['MA20'] + (df['Close'].rolling(window=20).std() * 2)

# 타점 생성 (볼린저 상단 돌파 후 안으로 들어올 때 매도, 50선 안착 매수 등)
buy_markers = np.full(len(df), np.nan)
sell_markers = np.full(len(df), np.nan)
buy_markers[60] = df['Low'].iloc[60] - 2  # 예: 50선 안착 매수
sell_markers[75] = df['High'].iloc[75] + 2 # 예: 볼린저 상단 매도

# 2. mpf 플롯 설정 (이평선, 볼린저 밴드, 매매 마커)
apdict = [
    mpf.make_addplot(df['MA20'], color='red', width=1.5, title="20선"),
    mpf.make_addplot(df['MA50'], color='blue', width=1.5, title="50선"),
    mpf.make_addplot(df['MA100'], color='green', width=1.5, title="100선"),
    mpf.make_addplot(df['BB_Upper'], color='gray', linestyle='dashed'),
    mpf.make_addplot(buy_markers, type='scatter', markersize=100, marker='^', color='orange'),
    mpf.make_addplot(sell_markers, type='scatter', markersize=100, marker='v', color='purple')
]

# 3. 렌더링
mpf.plot(df, type='candle', style='yahoo', addplot=apdict,
         title="09 매매법 & 이평/볼린저 전략",
         hlines=dict(yvals=[95, 105], colors=['g','r'], linestyle='--')) # 전고/전저점 필히 긋는다
```

- **B) 웹 검색 시나리오**:
  - **검색 키워드 템플릿**:
    - "1분봉 20선 50선 돌파 매수 차트"
    - "볼린저 밴드 상단 이탈 매도 차트"
    - "장초반 9시 50선 안착 매매"
  - 검색 결과 중 노트의 `20선, 50선 수렴 후 돌파` 또는 `볼린저 쌍바닥` 패턴이 가장 선명하게 보이는 실제 차트 이미지 1개만 보조 자료로 첨부한다.

## 3. 추출된 용어 사전 (JSON 포맷)