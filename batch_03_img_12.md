## 1. 본문 (차트 분석 및 매매 기법)

### 10/2 3분봉 매매 (거래량은 진짜)
- 1. 400선 {추정: 돌파 | 고가} 매매
- 2. 거래량 매매 (항시 {추정: 야간장 | 약간} 반응이 나온 거래량은 저점에 {추정: 최고 | 치고}, 매수)
- 3. 볼린저 매매 (볼린저 쌍바닥) (볼린저 수렴)
  - 뾰쪽이 : 400선. 볼린저 하단 이탈.
- 4. 쌍바닥 (W자형 캔들).

### 추세/구간 대응 전략
- 5. 우상향, 상승추세장에서 눌림을 받으면 400선 매매 한다
- 6. 하락추세가 강할 때는 기다려라
  - ① 120, 400선 매매가 돌때까지 기달림.
  - ② 100선 매매 하락때는 2번째 웅덩이 매수

### 오전장 (9시장) 거래 매매
- X. 9시장 거래 매매 (오전에 매수 / 100선 이탈선 긋는다-)
- 1. 9시 전에 캔들이 100선을 하락하고 돌파할때 매수
  - 캔들 흐름: V자 형태로 하락 후 반등하는 구간에 '매수타점'
  - 가로선으로 '100선 매매' 기준선을 표시
- 2. 상승후 전고점 가고 다시 하락 매수 (100선 매매확률 높다)

### 눌림 조건 (※ 눌림은)
- ① 볼린저 하단 이탈후 상승 (볼린저 수축하고 상승장일때)
- ② 20선 거래량 50선 거래량 {추정: 상향 돌파 | 하향 돌파}
- ③ 100선 하향 돌파


## 2. AI 답변 및 단계별 시각화 가이드 (Response & Rendering Guide)

### (1) 기본 답변 방식
- 답변 순서 템플릿:
  - (a) 시장상태/추세 요약 → (b) 핵심 근거(100선, 400선, 볼린저 밴드, 거래량 등 노트에 나온 지표) → (c) 진입 타점(9시장 돌파 매수, 2번째 웅덩이 등) → (d) 무효화 조건 → (e) 손절 → (f) 1차/2차 익절 → (g) 리스크/주의사항(하락추세 강할 때 대기 등)
- 표현 원칙:
  - 노트에 기재된 은어(뾰쪽이, 2번째 웅덩이, W자형 캔들 등)는 원문 그대로 사용.
  - 노트에 없는 보조지표(RSI, MACD 등)나 매매 조건은 임의로 추가하지 않는다.

### (2) 1단계 (빠른 시각화): ASCII 그래프 규칙
- 텍스트 기호를 활용해 캔들 흐름과 노트에 명시된 지표(100선, 볼린저 하단 등)를 표현한다.
- 지지선/저항선은 `---` 또는 `===`로 표기하고 가격 또는 지표명을 라벨링한다.
- 진입 타점에는 `[매수타점]` 등 노트에 기록된 명칭을 그대로 사용한다.

**[예시 ASCII 그래프 - 9시장 100선 돌파 매매]**
```text
(Price)
  |  \                   / (상승)
  |   \                 /
  |    \   [매수타점]  /
--|-----\------^------/------- 100선 매매 기준선
  |      \           /
  |       \__(V자)__/
  |
  +-------------------------------- (Time: 오전 9시 전후)
```

### (3) 2단계 (정밀 시각화): 파이썬 렌더링 또는 외부 실제 차트 첨부
- 사용자가 상세한 차트를 요구할 경우, 아래 파이썬 코드를 실행하여 가상의 캔들 차트를 렌더링하거나 웹 검색을 통해 유사한 실제 차트를 첨부한다.
- **파이썬 렌더링 구체 세팅 값**:
  - 캔들 타입: `candle` 고정
  - 봉 개수: 노트에 명시되지 않았으므로 default(40봉) 적용
  - 거래량 표시 여부: 조건에 따라 포함 (default: False)
  - 이평선 기간(mav): 100선, 400선 등 노트에 언급된 선 중심 (예시에서는 100선 적용)
  - 진입 마커 위치와 라벨: 캔들이 100선을 하락 이탈한 후 재돌파하는 시점에 '매수타점' 마커(^ 모양) 표기

**[파이썬 렌더링 코드 예시]**
```python
import pandas as pd
import numpy as np
import mplfinance as mpf

# 1. 가상의 3분봉 캔들 데이터 생성 (9시 전후 V자 하락 및 돌파 패턴)
dates = pd.date_range('2023-10-02 08:30', periods=40, freq='3min')
open_prices = np.linspace(110, 95, 20).tolist() + np.linspace(95, 110, 20).tolist()
close_prices = np.linspace(108, 93, 20).tolist() + np.linspace(97, 112, 20).tolist()
high_prices = [max(o, c) + 2 for o, c in zip(open_prices, close_prices)]
low_prices = [min(o, c) - 2 for o, c in zip(open_prices, close_prices)]

df = pd.DataFrame({
    'Open': open_prices,
    'High': high_prices,
    'Low': low_prices,
    'Close': close_prices
}, index=dates)

# 2. 100선 이동평균선 가상 데이터 생성 (노트의 100선 매매 기준)
df['MA100'] = np.linspace(105, 103, 40)

# 3. 매수타점 (100선을 하락 이탈 후 다시 돌파하는 시점)
buy_markers = [np.nan] * len(df)
buy_idx = 28 # 재돌파 시점 추정
buy_markers[buy_idx] = df['Low'].iloc[buy_idx] - 3

# 4. 차트 렌더링
ap_ma = mpf.make_addplot(df['MA100'], color='blue', width=1.5)
ap_buy = mpf.make_addplot(buy_markers, type='scatter', markersize=100, marker='^', color='red')

mpf.plot(df, type='candle', addplot=[ap_ma, ap_buy], style='charles',
         title='9 AM Market: 100 MA Breakdown & Re-breakout (Buy Point)',
         ylabel='Price',
         volume=False)
```

- **웹 검색 시 키워드 템플릿**:
  - "3분봉 100일선 지지 반등 차트"
  - "주식 오전장 볼린저밴드 하단 쌍바닥 패턴"
  - 검색된 차트 중 V자형 또는 W자형 패턴이 가장 명확한 1개만 보조 자료로 첨부한다.


## 3. 추출된 용어 사전 (JSON 포맷)