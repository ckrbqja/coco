## 1. 본문 (차트 분석 및 매매 기법)

### 09시 전후 및 기본 진입 조건
- 09시전 거래량 {추정: 수반 | 터진} {추정: 급등 | 가장} 엄청난 폭포 후
- 주로 400선 또는 브이왑선 돌파할때 매수
- 09시 급상승이 나올때 1차 매수

### 주요 이동평균선/지표 지지 및 돌파 매수
- 브이왑, 또는 20선, 60선 돌파할때 매수
- 3분봉 지지선 브이왑 매수 (60선도 함께본다)
- 우상향 20선 매수
- 100선 매수
- 400선 매수 : 400선 지지받고 매수
- 400선 돌파시 매수한다

### 어깨선 및 패턴 매수
- 3파 하락이 400선 어깨선에 매수
- 돌파 400선 돌파 매수 (브이왑)
- 브이왑 아래 어깨선 매수한다
- 400선 아래 어깨선 매수 (20선 만남)
- 20선 쌍바닥, 어깨선 매수 (엔벨로프)
- 횡보 위아래 어깨선 돌파할때 매수
- 어깨선에서 매수

### 특정 상황 매매 (급등/상한가)
- 급등 매수 : 1차급등 20선 눌림목 하단 브이왑 계속돌파
- 상한가 매수 : 전일 상한가 종목은 다음날 무조건 본다
- 브이왑, 400선 돌파할때 매수

### 기타 참고 사항
- 매수구간
- {추정: 조건 종목 | 조건 검색} 0904

## 2. AI 답변 및 단계별 시각화 가이드 (Response & Rendering Guide)

(1) 기본 답변 방식
- 답변 순서 템플릿:
  - (a) 시장상태/추세 요약: 09시 전후, 급상승, 3파 하락, 횡보, 급등, 전일 상한가 등 노트에 명시된 상황 파악
  - (b) 핵심 근거: 거래량, 3분봉, 브이왑(VWAP), 20선, 60선, 100선, 400선, 어깨선, 쌍바닥, 엔벨로프 등
  - (c) 진입 타점: 어깨선 매수, 400선/브이왑 지지 또는 돌파 시 매수, 20선/100선 우상향 매수 등
  - (d) 무효화 조건: 노트 원문에 명시되어 있지 않으므로 생략
  - (e) 손절: 노트 원문에 명시되어 있지 않으므로 생략
  - (f) 1차/2차 익절: 노트 원문에 명시되어 있지 않으므로 생략
  - (g) 리스크/주의사항: 노트 원문에 명시되어 있지 않으므로 생략
- 표현 원칙:
  - 브이왑, 400선, 3파 하락, 어깨선 등 원문의 은어 및 축약어를 그대로 사용한다.
  - 노트에 없는 손절/익절 규정이나 보조지표(예: RSI, MACD 등)는 절대 추가하여 설명하지 않는다.

(2) 1단계 (빠른 시각화): ASCII 그래프 규칙
- 사용자가 기본 설명을 요청할 경우 텍스트 기반의 ASCII 그래프를 출력한다.
- 캔들 흐름은 `\`, `/`, `v` 기호로 3파 하락 및 상승 돌파 등을 표현한다.
- 주요 이평선(20, 60, 100, 400선)과 브이왑은 `~`, `-`, `=` 등을 사용해 선을 긋고 우측에 라벨을 표기한다.
- 지지/저항/어깨선은 `---`로 표기한다.
- 진입 타점은 `[매수]` 또는 `*`로 마킹한다.

```text
[ASCII 시각화 예시: 3파 하락 및 400선 어깨선 매수]

주가 흐름:   \      / \          / 
 (3파 하락)   \    /   \        /  (우상향)
               \  /     \      /  [매수] (브이왑 돌파)
                 v       \    / 
                          \  / 
어깨선 -------------------- v -- [매수] (20선 만남/어깨선 지지)
                            
400선 ==========================================
브이왑 ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
```

(3) 2단계 (정밀 시각화): 파이썬 렌더링 또는 외부 실제 차트 첨부
- 사용자가 "자세히 그려줘" 또는 "정확한 차트를 보여줘"라고 요청 시, A) 파이썬 가상 차트 렌더링 또는 B) 외부 실제 차트 검색을 수행한다.

A) 파이썬(mplfinance) 코드 렌더링 지침
- 캔들 타입: `type='candle'` 고정
- 봉 개수: 노트에 명시되지 않았으므로 default(80봉) 적용
- 거래량 표시 여부: 노트 상단에 '거래량 수반'이 언급되므로 `volume=True` 적용
- 이평선 기간(mav): 노트에 등장하는 20, 60, 100, 400 적용
- 지지/저항선 위치: 어깨선 및 쌍바닥 라인을 수평선(hlines)으로 표기
- 진입 마커: 400선 지지 구간 및 브이왑 돌파 구간에 `^` 마커 표기
- 차트 제목: "3분봉 지지선 브이왑 및 어깨선 매수"

```python
import pandas as pd
import numpy as np
import mplfinance as mpf

# 1. 가상의 3분봉 데이터 생성 (3파 하락 후 브이왑/400선 지지 및 반등 형태)
np.random.seed(42)
dates = pd.date_range(start='2023-09-04 09:00', periods=80, freq='3T')
close_prices = np.linspace(5000, 4000, 30) # 1파 하락
close_prices = np.append(close_prices, np.linspace(4000, 4400, 15)) # 반등
close_prices = np.append(close_prices, np.linspace(4400, 3900, 15)) # 3파 하락 (어깨선 형성)
close_prices = np.append(close_prices, np.linspace(3900, 4800, 20)) # 우상향 급상승 및 브이왑 돌파

high = close_prices + np.random.uniform(10, 50, 80)
low = close_prices - np.random.uniform(10, 50, 80)
open_prices = close_prices + np.random.uniform(-20, 20, 80)
volume = np.random.randint(1000, 50000, 80)

# 09시전 거래량 터진 급등 상황을 위한 시작점 거래량 증폭
volume[0:5] = volume[0:5] * 5 

df = pd.DataFrame({'Open': open_prices, 'High': high, 'Low': low, 'Close': close_prices, 'Volume': volume}, index=dates)

# 2. 이동평균선(20, 60, 100) 및 브이왑(가상) 계산 (400선은 데이터 부족으로 상수값 처리)
df['MA20'] = df['Close'].rolling(window=20, min_periods=1).mean()
df['MA60'] = df['Close'].rolling(window=60, min_periods=1).mean()
df['VWAP'] = (df['Close'] * df['Volume']).cumsum() / df['Volume'].cumsum()
line_400 = 3850 # 가상의 400선

# 3. 매수 타점 설정 (어깨선 부근 지지점 및 브이왑 돌파점)
buy_signals = [np.nan] * len(df)
buy_signals[59] = df['Low'].iloc[59] - 50 # 3파 하락 후 어깨선 매수 타점
buy_signals[70] = df['Low'].iloc[70] - 50 # 브이왑 돌파 타점

# 4. 차트 렌더링 설정
apdict = [
    mpf.make_addplot(df['MA20'], color='blue', width=1.5, panel=0),
    mpf.make_addplot(df['MA60'], color='green', width=1.5, panel=0),
    mpf.make_addplot(df['VWAP'], color='purple', width=2.0, panel=0),
    mpf.make_addplot(buy_signals, type='scatter', markersize=100, marker='^', color='red', panel=0)
]

# 어깨선 및 400선 수평선 추가
hlines = dict(hlines=[4000, line_400], colors=['orange', 'red'], linestyle=['--', '-'], linewidths=[1.5, 2])

mpf.plot(df, type='candle', volume=True, addplot=apdict, hlines=hlines,
         title='3분봉 지지선 브이왑 및 어깨선 매수', style='charles', figsize=(12, 8))
```

B) 외부 실제 차트 검색 활용 시 지침
- 검색 키워드 템플릿:
  1. "주식 3분봉 브이왑 돌파 매수 차트"
  2. "주식 3파 하락 후 쌍바닥 어깨선 매수"
  3. "엔벨로프 20선 쌍바닥 매수 차트"
- 노트에 기술된 패턴(3파 하락 후 400선/브이왑 지지 및 반등)과 가장 일치하는 차트 이미지 1개만 결과에 첨부하여 보조 자료로 제공한다.

[
  {"term":"3파 하락","definition":"엘리어트 파동 이론 등에서 나타나는 하락 3파동(ABC 하락 등)을 의미하며, 추세 전환 전의 마지막 하락 구간을 뜻함"},
  {"term":"엔벨로프","definition":"이동평균선을 중심으로 위아래로 일정한 비율만큼 떨어진 선을 그린 보조지표(Envelope)"},
  {"term":"급등 매수","definition":"단기간에 주가가 빠르게 상승하는 종목을 특정 조건(눌림목 등)에서 진입하는 매매 기법"},
  {"term":"1차급등","definition":"주가가 바닥권이나 횡보 구간을 탈피하여 첫 번째로 강하게 상승하는 구간"},
  {"term":"눌림목 하단","definition":"상승 추세 중 일시적으로 하락하는 눌림목 구간의 가장 낮은 지지 영역"},
  {"term":"상한가 매수","definition":"전일 또는 당일 일일 가격 제한폭(상한가)까지 도달한 강한 종목을 대상으로 다음 날 연속적인 상승을 기대하고 진입하는 매매 기법"},
  {"term":"조건 종목 0904","definition":"작성자가 특정 조건 검색식이나 관심 종목 그룹을 분류하기 위해 사용한 날짜(9월 4일) 또는 고유 코드 명칭"}
]