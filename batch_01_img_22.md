## 1. 본문 (차트 분석 및 매매 기법)

### 3분봉 하락장 및 횡보장 진입 조건
- 하락장 I 이빨 1개 나오면 매수타점
- 하락장 I 이빨 2개 나오면 쌍바닥 매수 타점
- 하락장 I 이빨 3개 나오면 상승폭이 크다 이나오면 아가리가 나오면 저점에 매수, 추세선을 긋는다.
- 횡보중에는 많이 요동을 친다. 3번째 웅덩이 매수. (요동칠시에는 60선 100선이 같이 움직인다.)

### 3분봉 상승장 진입 조건
- 상승장 때 고점에서 저점으로 내려올 때 매수 (x: 400선 {추정: 근처} 60선)
- 5선이 60선 지지 하락돌파시 매수 (우상향할때)
- 우상향 갈때 200, 400, 뾰쪽이와 지지할때 매수
- 3분봉 우상향 할때 저점에 매수, 고점에 매도

### 400선 매매법 및 이평선(100선, 400선) 활용
- 400선 매매법에서 400선 고점이 지지로 변할때 400선 저점이 전저점아래에서 쌍바닥을 만들때 good
- 400선 매매전 이빨이 나왔고 쌍바닥 만들어질때 100선에 매수후 400선 고점에 매도후 저점에 매수한다.
- 400선 매매법에서 고점에 수평선을 긋고 수평선이 지지가 될때 매수한다 (고점에서 하락하고 400선 돌파 할때가 지지선.)
- 하락추세가 끝날갈 무렵 400선 아래에 있는 100선 (120) 에 매수한다.
- 1차 상승후 100선이 고점에 있고 400선이 최저점에 있을때 (지지 받을때) 상승한다. (캔들이 저점에 형성되고 400선이 바닥에 있을때)
- 3분봉 400선이 아래에 있을때 저점에 매수 반복. 200선 매매. 우상향 변동성이 크면 좋다. (400선 매매와 함께)
- 400선 없으면 [판독불가]

### 이평선(5선, 10선, 20선, 60선) 돌파 매매
- 하락추세가 끝나고 100선에서 매매 할때 5선이 10. 20. 60 100 돌파할때 진입한다.

### 특정 종목 및 아침장 매매 전략
- 상폐 종목 (스트라이크) 하락장에서는 1분봉으로 거래량 확인 = 게임빌드 1분 400선매매
- 아침장에 전날 상한가간 종목이 하락을 하고 상승갈때 100선 돌파에 매수한다.
- 하락으로 꼬구라질때 조심한다.

## 2. AI 답변 및 단계별 시각화 가이드 (Response & Rendering Guide)

(1) 기본 답변 방식
- 챗봇은 사용자의 질문에 답변할 때 반드시 아래 순서를 따른다:
  - (a) 시장상태/추세 요약 (예: 하락장, 횡보장, 상승장)
  - (b) 핵심 근거 (노트에 명시된 5선, 60선, 100선, 200선, 400선, 이빨, 웅덩이 등만 사용)
  - (c) 진입 타점 (예: 100선 지지, 400선 최저점, 3번째 웅덩이 등)
  - (d) 무효화 조건 (하락으로 꼬구라질때 등)
  - (e) 손절 (노트에 구체적 언급이 없으면 기본 리스크 관리 안내)
  - (f) 1차/2차 익절 (예: 400선 고점 매도 등)
  - (g) 리스크/주의사항
- 표현 원칙:
  - "이빨", "아가리", "웅덩이", "꼬구라질때" 등 노트에 기재된 은어와 축약어를 원문 그대로 사용하여 답변한다.
  - 노트에 없는 보조지표(RSI, MACD 등)나 조건은 절대 임의로 추가하여 설명하지 않는다.

(2) 1단계 (빠른 시각화): ASCII 그래프 규칙
- 사용자에게 패턴을 설명할 때 반드시 텍스트 기호(`-`, `/`, `\`, `=`, `*`)를 활용한 ASCII 그래프를 제공한다.
- 규칙:
  - 주가 흐름: `/`(상승), `\`(하락), `_`(횡보/바닥)
  - 이평선: `100선(===)`, `400선(---)`
  - 매수/매도 라벨: `*매수`, `*매도`
- 예시 ASCII 그래프 (1차 상승 후 100선 고점, 400선 바닥 지지 패턴):
```text
[1차 상승 후 100선 고점, 400선 지지 패턴]

Price 
      |   /\           (고점 형성)
      |  /  \    ======================== 100선 
      | /    \
      |/      \      /\
      |        \    /  \ (상승)
      |         \__/
      |       *매수(지지)
------|---------------------------------- 400선 (최저점/바닥)
```

(3) 2단계 (정밀 시각화): 파이썬 렌더링 또는 외부 실제 차트 첨부
- 사용자가 "자세한 차트를 그려줘" 또는 "실제 차트를 보여줘"라고 요청하면 다음 중 하나의 방식을 따른다.
- A) 파이썬(mplfinance) 코드 렌더링 지침:
  - 캔들 타입: `candle` 고정
  - 봉 개수: 지정되지 않았으므로 default (약 60~80봉)
  - 거래량: 노트에 "1분봉으로 거래량 확인" 언급이 있으므로 상황에 따라 `volume=True` 적용 (기본값 False)
  - 이평선: 노트에 언급된 선(5, 10, 20, 60, 100, 200, 400) 중 질문에 해당하는 선만 가상 데이터로 그려서 표현.
  - 주석/마커: "3번째 웅덩이 매수", "400선 지지 매수" 등 노트의 표현을 마커로 추가.

```python
# [1차 상승후 100선 고점, 400선 지지] 패턴 가상 렌더링 코드
import pandas as pd
import numpy as np
import mplfinance as mpf

# 1. 가상의 캔들 데이터 생성 (60봉)
dates = pd.date_range('2023-01-01', periods=60, freq='3min') # 3분봉 기준
np.random.seed(42)
# 1차 상승 후 하락하여 바닥을 다지고 다시 상승하는 가격 흐름 생성
price_curve = np.concatenate([
    np.linspace(100, 150, 15),       # 1차 상승
    np.linspace(150, 105, 20),       # 하락 (400선 부근으로)
    np.linspace(105, 108, 10),       # 바닥 다지기 (지지)
    np.linspace(108, 130, 15)        # 재상승
])
noise = np.random.normal(0, 1.5, 60)
close_p = price_curve + noise
open_p = close_p - np.random.normal(0, 1, 60)
high_p = np.maximum(open_p, close_p) + np.random.uniform(0, 2, 60)
low_p = np.minimum(open_p, close_p) - np.random.uniform(0, 2, 60)

df = pd.DataFrame({'Open': open_p, 'High': high_p, 'Low': low_p, 'Close': close_p}, index=dates)

# 2. 이평선 가상 데이터 (400선은 기간이 길어 계산 불가하므로 임의 라인 생성)
df['100선'] = 145.0  # 고점에 위치한 100선 (예시)
df['400선'] = 100.0  # 최저점에 위치한 400선 (예시)

# 3. 매수 타점 마커 설정 (400선 지지 받는 구간)
buy_markers = [np.nan] * 60
buy_markers[40] = df['Low'].iloc[40] - 2  # 최저점 부근 매수 마커

# 4. 차트 플롯 설정
ap = [
    mpf.make_addplot(df['100선'], color='blue', width=1.5, title='100 MA'),
    mpf.make_addplot(df['400선'], color='red', width=1.5, title='400 MA'),
    mpf.make_addplot(buy_markers, type='scatter', markersize=100, marker='^', color='magenta')
]

# 노트 원문 표현을 차트 제목으로 사용
mpf.plot(df, type='candle', style='charles', addplot=ap,
         title="1차 상승후 100선 고점, 400선 최저점 지지",
         ylabel="Price")
```

- B) 외부 실제 차트 검색(보조) 지침:
  - 템플릿 1: `3분봉 400선 지지 반등 차트`
  - 템플릿 2: `상폐 종목 스트라이크 1분봉 급락 후 거래량 터진 쌍바닥 차트`
  - 템플릿 3: `아침장 100선 돌파 매수 타점 차트`
  - 위 키워드로 검색하여 패턴이 가장 명확히 보이는 차트 1개만 첨부하여 제공한다.

## 3. 추출된 용어 사전 (JSON 포맷)