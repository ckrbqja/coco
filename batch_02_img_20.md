## 1. 본문 (차트 분석 및 매매 기법)

### 진입 조건 및 타점
- 최저가 횡보중 20선에 쌍바닥을 만들고 100선 돌파갈때 매수 한다
- 이평선 뾰쪽이 매수
- 400선 아래 100선 매수
- 정배열로 상승가는 종목 예) 400선위에 캔들이 200선 지지 받을때 매수 타점
- 200선위에 캔들이 나오면 120선 지지에 닿을때 매수
- 거래량 나온후 저점매수
- 100선 아래 쌍바닥 매수 (5일선, 20선 돌파갈때)
- 전고점이 눌림지지선이 될때 쌍바닥 매수

### 추세 및 기준 확인
- 15분봉 상승추세, 하락추세 확인
- 거래량 확인
- 하락추세에 이빨이 3개 나왔는지
- 어깨선, 100선은 400선 아래에서 2파동 이다
- 전저점 지지 받는지 (쌍바닥)
- 20선 타고 상승하는지 (정배열)
- 400선 매매 ① 고점, 저점, 지지 받을때 ② 거래량이 나올때 확인 1분봉으로 확인

### 지지/저항 및 구간 매매
- 위 60선 매매 / 기본선 100 / 아래 120선 매매 (매매는 같다)
- 눌림목 매매 (쌍바닥) 상승후 고점에서 하락추세 바닥에서 100선 아래 쌍바닥
- 전고점이 하락 지지를 받을때
- 상한가 눌림에서 장기이평을 하락하고 돌파할때 또는 지지받을때
- 상한가 전고점이 눌림일때 장기이평을 만날때 (아래에서 올라오는)

### 기타 메모
- 해외선물 AI는 거의 기계 수준인듯 하고 차라리 알트코인은 나아서 국내로 보낸다

## 2. AI 답변 및 단계별 시각화 가이드 (Response & Rendering Guide)

(1) 기본 답변 방식
- 답변 순서 템플릿:
  - (a) 시장상태/추세 요약 (15분봉/1분봉 추세 및 정배열 여부)
  - (b) 핵심 근거 (노트에 기재된 5일선, 20선, 60선, 100선, 120선, 200선, 400선, 거래량 등)
  - (c) 진입 타점 (쌍바닥, 이평선 뾰쪽이, 특정 이평선 지지/돌파 시점)
  - (d) 무효화 조건 (전저점/눌림지지선 이탈 시)
  - (e) 손절 (지지선 붕괴 시)
  - (f) 1차/2차 익절 (다음 장기이평선 저항 구간)
  - (g) 리스크/주의사항 (거래량 동반 여부 반드시 확인)
- 표현 원칙:
  - '쌍바닥', '이평선 뾰쪽이', '이빨이 3개', '정배열', '눌림목' 등 섹션 1의 은어/축약어를 그대로 사용한다.
  - 노트에 언급되지 않은 MACD, RSI 등의 보조지표는 절대 추가하지 않는다.

(2) 1단계 (빠른 시각화): ASCII 그래프 규칙
- 일반적인 설명 요청 시, 텍스트 기호를 활용한 ASCII 그래프를 반드시 출력한다.
- ASCII 그래프 규칙:
  - 캔들 흐름(상승/하락/횡보): `\`, `/`, `-`, `W` 기호를 사용하여 표현한다.
  - 이평선(노트에 나온 선): `~` (단기), `=` (중장기) 기호 등으로 구분하고 라벨을 붙인다.
  - 지지/저항선 및 가격 라벨: `---` 점선과 텍스트로 표기한다.
  - 진입/손절/익절 마커: `[매수]`, `[손절]`, `[익절]` 텍스트로 타점 위치에 표시한다.
- 예시 ASCII 그래프 (100선 아래 20선 쌍바닥 및 돌파 패턴):
```text
      100선 ======================================/==== (100선 돌파)
                                                 /
                                                / [매수]
        20선 ~~~~~~\              /~~~~\       /~~~~~
                    \            /      \     /
(최저가 횡보)        \          /        \   /
-----------------------\--------/-----------\-/--------- 전저점 지지선
                       (쌍바닥1)            (쌍바닥2)
```

(3) 2단계 (정밀 시각화): 파이썬 렌더링 또는 외부 실제 차트 첨부
- 사용자가 "자세히 그려줘", "정확한 차트를 보여줘"라고 요청할 경우 다음 중 하나의 방식을 따른다:
  A) 파이썬(mplfinance) 코드로 "가상의 캔들 차트" 렌더링
  B) 웹 검색으로 해당 패턴(예: 100선 아래 쌍바닥 돌파)과 유사한 "외부 실제 차트 사진" 검색 후 첨부
- 파이썬 재현 시 구체 세팅 값:
  - 캔들 타입: `candle`
  - 봉 개수: 80봉 (default)
  - 거래량 표시 여부: 노트에 거래량 확인 조건이 있으므로 `True`로 설정
  - 이평선 기간(mav): (20, 100) 적용 (노트 핵심 타점 기준)
  - 지지/저항선: 전저점 부근에 수평선(hlines) 표기
  - 진입 마커 위치: 20선 쌍바닥 형성 후 100선을 돌파하는 양봉 위치에 '매수' 화살표 및 텍스트 추가
  - 차트 제목: "100선 아래 20선 쌍바닥 돌파 매매"

```python
# 파이썬 렌더링 예시 코드 (100선 아래 20선 쌍바닥 돌파)
import pandas as pd
import numpy as np
import mplfinance as mpf

# 1. 가상의 캔들 데이터 생성 (80봉)
np.random.seed(42)
dates = pd.date_range('2023-01-01', periods=80)
data = {'Open': [], 'High': [], 'Low': [], 'Close': [], 'Volume': []}

price = 1000
for i in range(80):
    # 하락 -> 1차 바닥 -> 반등 -> 2차 바닥(쌍바닥) -> 상승(100선 돌파) 형태 구현
    if i < 20: price -= np.random.randint(5, 20)      # 하락
    elif i < 35: price += np.random.randint(5, 15)    # 1차 반등
    elif i < 50: price -= np.random.randint(5, 15)    # 2차 하락 (쌍바닥)
    else: price += np.random.randint(10, 25)          # 상승 돌파

    open_p = price + np.random.randint(-5, 5)
    close_p = price + np.random.randint(-5, 5)
    high_p = max(open_p, close_p) + np.random.randint(2, 10)
    low_p = min(open_p, close_p) - np.random.randint(2, 10)
    vol = np.random.randint(1000, 5000)
    
    # 돌파 구간 거래량 증가
    if i > 60: vol += np.random.randint(5000, 10000)

    data['Open'].append(open_p)
    data['High'].append(high_p)
    data['Low'].append(low_p)
    data['Close'].append(close_p)
    data['Volume'].append(vol)

df = pd.DataFrame(data, index=dates)

# 2. 이동평균선(20선, 100선) 설정
# 가상 데이터 80봉이므로 100선은 단순화를 위해 임의의 하락 추세선으로 대체 계산
df['MA20'] = df['Close'].rolling(window=20).mean()
df['MA100'] = np.linspace(1100, 950, 80) # 노트의 하락하는 100선 모사

# 3. 매수 타점 마커 설정 (100선 돌파 구간)
buy_signals = np.full(len(df), np.nan)
buy_signals[65] = df['Low'].iloc[65] - 15 # 돌파 캔들 하단에 매수 마커

apds = [
    mpf.make_addplot(df['MA20'], color='blue', width=1.5, title='20 MA'),
    mpf.make_addplot(df['MA100'], color='orange', width=2, title='100 MA'),
    mpf.make_addplot(buy_signals, type='scatter', markersize=100, marker='^', color='red')
]

# 4. 차트 렌더링 (거래량 포함)
mpf.plot(df, type='candle', volume=True, addplot=apds, 
         title='100 MA Breakout after Double Bottom', 
         style='charles', figsize=(10, 6))
```

- 웹 검색을 쓸 경우:
  - 검색 키워드 템플릿: "주식 20일선 쌍바닥 100일선 돌파 차트", "정배열 눌림목 120선 지지 차트"
  - 가장 시각적으로 뚜렷한(이평선 지지 및 쌍바닥 패턴이 명확한) 1개의 차트 이미지만 첨부한다.

## 3. 추출된 용어 사전 (JSON 포맷)