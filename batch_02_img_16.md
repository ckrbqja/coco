## 1. 본문 (차트 분석 및 매매 기법)

### 1분봉 존버 매매 
- 볼린저 수렴구간에 이평선들 3개이상 모이고 5선이 20선을 뚫고가면서 부채꼴 만들어질때 매수
- 9/18 1분 정배열에서
- 상승하는 400선을 지지받고 위로 올라갈때 (캔들이) 매수
- 볼린저 상단찍고 올때 매도

### 핵심 이동평균선 원칙
- *** 100선 우상향일때 매매 할것
- " 하향 " NO
- 20선 생명선

### 상승장 10, 20 추세 매매법 (9/14)
- 1. 400선 200선 이 정배열 우상향 할때
  - 20선 안착후 상승할때 매수
- 2. 전고점이 하락하는 캔들이 지지선이 될때
- 3. 볼린저 하단
  - X : -15% 이상이면 20선매수
- 4. 전고점이 지지선이 될때
- 5. 하락장일때 20선, 볼린저 하단, 전고점 지지
- 6. 계단식 매매

### 자주나오는 패턴 (400선 매매)
- 주가가 하락하다가 400선을 돌파할 때 1차 '돌파매수'
- 400선 근처까지 다시 내려와 '지지'를 받을 때 '2차매수'
- 400선이 주요 지지선 역할을 하는 V자/N자 반등 패턴

### 이평 수렴 후 단타 기법
- 8. 5, 10, 20, 60 이평 수렴후 모여 상승할때 매수
- (단, 120, 400) 역배열 상태
- 예) 2시이후 9:15분 경
- -15% 이내 단타용
- 이때 120, 200선은 아직 정배열 {추정: 이어도 상관없다 | 이면 상관없다}

## 2. AI 답변 및 단계별 시각화 가이드 (Response & Rendering Guide)

(1) 기본 답변 방식
- 답변 순서 템플릿:
  - (a) 시장상태/추세 요약: 1분봉 정배열, 100선 우상향 등 노트에 기록된 시장 환경 명시
  - (b) 핵심 근거: 볼린저 수렴, 이평선 3개 이상 밀집, 5선이 20선 상향 돌파(부채꼴 발산) 등
  - (c) 진입 타점: 400선 지지 후 상승 시 매수, 400선 돌파매수 및 지지 시 2차매수 등
  - (d) 무효화 조건: 100선 하향 시 진입 금지
  - (e) 손절: 노트에 명시된 생명선(20선) 이탈 추정 (노트 원문 기반으로만 안내)
  - (f) 1차/2차 익절: 볼린저 상단 찍고 올 때 매도
  - (g) 리스크/주의사항: -15% 이내 단타용 시간대(2시 이후, 9:15분 경) 준수
- 표현 원칙:
  - "존버 매매", "부채꼴", "생명선", "400선 매매" 등 섹션 1의 용어를 원문 그대로 사용한다.
  - 노트에 없는 보조지표(RSI, MACD 등)나 추가 매매 룰을 절대 임의로 덧붙이지 않는다.

(2) 1단계 (빠른 시각화): ASCII 그래프 규칙
- 사용자의 일반적인 설명 요청 시 텍스트 기호로 구성된 ASCII 그래프를 반드시 출력한다.
- 캔들의 흐름은 `\` (하락), `/` (상승), `-` (횡보/수렴) 기호로 표현한다.
- 주요 이평선(400선 등)은 `===` 또는 `---` 선으로 표현하고 라벨을 붙인다.
- 매수/매도 타점은 `[매수]`, `[매도]`, `[돌파매수]`, `[2차매수]` 마커로 표시한다.

```text
[400선 매매 자주나오는 패턴 예시]

       주가 하락           [돌파매수]               상승 지속
        \                    /                       /
         \                  /      [2차매수]        / 
          \                /          / \          /
           \              /          /   \        /
============\============/==========/=====\======/=========== [400선]
             \          /     (지지)       \    /
              \        /                    \  /
               \      /                      \/
                \    /
                 \  /
                  \/ (바닥 형성)
```

(3) 2단계 (정밀 시각화): 파이썬 렌더링 또는 외부 실제 차트 첨부
- 사용자가 "자세히 그려줘", "정확한 차트를 보여줘"라고 요청할 경우, 파이썬 코드(`mplfinance` 활용)를 제공하여 가상의 캔들 차트를 렌더링하거나 웹 검색을 통해 유사한 차트 사진 1장을 첨부한다.
- 파이썬 렌더링 세팅 규칙:
  - 캔들 타입: `candle`
  - 봉 개수: default(80봉)
  - 거래량 표시: default(False)
  - 이평선 기간(mav): 5, 10, 20, 60, 100, 120, 200, 400선 중 전략에 해당하는 선만 `make_addplot`으로 겹쳐 그린다.
  - 볼린저 밴드: 상단, 하단을 구상하여 수렴(스퀴즈) 후 발산하는 형태를 모의 데이터로 구성한다.
  - 진입 마커: 차트상 400선 터치/반등 부근에 '돌파매수', '지지 2차매수' 라벨링을 추가한다.

```python
import pandas as pd
import numpy as np
import mplfinance as mpf

# 1. 가상의 1분봉 데이터 생성 (80봉 default)
dates = pd.date_range('2023-10-01 09:00', periods=80, freq='1min')
df = pd.DataFrame(index=dates)

# 주가가 400선 아래로 내려갔다가 돌파 후 지지받는 N자 패턴 모의
base_price = np.concatenate([
    np.linspace(105, 95, 20),   # 하락
    np.linspace(95, 102, 20),   # 반등 (돌파매수 구간)
    np.linspace(102, 99, 15),   # 눌림 (400선 지지)
    np.linspace(99, 110, 25)    # 재상승 (2차매수 후 상승)
])
df['Close'] = base_price + np.random.uniform(-0.5, 0.5, 80)
df['Open'] = df['Close'].shift(1).fillna(105)
df['High'] = df[['Open', 'Close']].max(axis=1) + np.random.uniform(0.1, 1, 80)
df['Low'] = df[['Open', 'Close']].min(axis=1) - np.random.uniform(0.1, 1, 80)

# 2. 이동평균선 및 기준선 세팅 (노트 기반)
# 400선은 핵심 지지/저항선이므로 수평선으로 가정
df['MA400'] = 100 
df['MA20'] = df['Close'].rolling(window=20, min_periods=1).mean()
df['MA5'] = df['Close'].rolling(window=5, min_periods=1).mean()

# 3. 차트 시각화 요소 추가
add_plots = [
    mpf.make_addplot(df['MA400'], color='black', width=2, linestyle='--', label='400MA'),
    mpf.make_addplot(df['MA20'], color='blue', width=1.5, label='20MA (생명선)'),
    mpf.make_addplot(df['MA5'], color='red', width=1.5, label='5MA')
]

# 4. 차트 렌더링 실행
# 주석: 400선을 상향 돌파할 때 '돌파매수', 이후 다시 400선 부근으로 내려와 지지받을 때 '2차매수'
mpf.plot(df, type='candle', addplot=add_plots, volume=False, 
         title="1 Min Chart: 400MA Breakout & Support", style='yahoo')
```
- 웹 검색 활용 시 검색 키워드 템플릿:
  - "주식 400일선 지지 반등 차트"
  - "이동평균선 수렴 후 정배열 발산 차트"
  - 검색된 결과 중 노트의 그림(V자 하락 후 400선 돌파 및 지지)과 가장 일치하는 차트 1개만 첨부한다.

## 3. 추출된 용어 사전 (JSON 포맷)