## 1. 본문 (차트 분석 및 매매 기법)

### 거래량 및 기본 추세
- 거래량 : 1분에 선긋고 거래량이 매수한다
- 우상향 : 20선 우상향하며 상승이 지속된다 매수(장)
- 전고.전저 : 고점 N, 저점 W... 매수

### 이동평균선 및 보조지표 패턴
- 볼린저 : 수축할때, 쌍바닥
- 뾰족이 : 200, 400, 120 우상향할때 나오면 매수
- 쌍바닥 : 20선
- 400선 스마일 : 돌파후 하락 지지받을때, 돌파할때 매수

### 100선 매매 기법
- 100선 : 5분, 1, 3분봉에서 잘맞는다
- N형 : 하락바닥이후 1차 양봉이 120선 돌파후 하락, 2번째 돌파할때 상승추세 이때 매수한다
- 계단형 : 상승중 눌림에 100선 하락, 돌파, 지지 매수
- 아래에서 올라올때 100선 돌파할때
- 100선 돌파, 하락, 돌파할때 매수
- 9시장에 100선에 선긋는다
- 100선 1차 우상향, 하락, 상승추세 거래량보고 매수(장)

## 2. AI 답변 및 단계별 시각화 가이드 (Response & Rendering Guide)

(1) 기본 답변 방식
- 챗봇 답변 순서 템플릿:
  - (a) 시장상태/추세 요약 → (b) 핵심 근거(100선, 120선, 20선, 거래량, N형/W형 등 노트에 명시된 지표) → (c) 진입 타점 (돌파, 지지, 쌍바닥 등) → (d) 무효화 조건 → (e) 손절 → (f) 1차/2차 익절 → (g) 리스크/주의사항
- 표현 원칙:
  - '400선 스마일', '뾰족이', '고점 N', '저점 W', '100선 돌파' 등 노트에 기록된 원본 용어와 축약어를 그대로 사용하여 설명한다.
  - 노트에 언급되지 않은 보조지표(RSI, MACD 등)나 피보나치 비율 등은 절대 추가하지 않는다.

(2) 1단계 (빠른 시각화): ASCII 그래프 규칙
- 사용자 요청 시 텍스트 기반 ASCII 그래프를 출력하여 직관적인 패턴 이해를 돕는다.
- ASCII 그래프 규칙:
  - 가격 흐름은 `*`, `+`, `/`, `\` 등의 기호로 표현한다.
  - 이동평균선(100선, 120선 등)은 `-`, `=`, `~` 기호로 캔들과 겹치게 또는 하단/상단에 배치한다.
  - 타점(매수)은 `[매수]` 또는 `^` 기호로 명시한다.
- 예시 ASCII 그래프 (N형 120선/100선 돌파 패턴):
```text
[N형 120선 돌파 및 2번째 돌파 매수 패턴]

가격 흐름:     하락바닥 --> 1차 양봉(120선 돌파) --> 하락(눌림) --> 2번째 돌파(상승추세)
               
      *                        * (고점 형성 후 눌림)            * * * [매수!]
     * *                      * *                              *
    *   *                    *   *                            * 
120선 ~~~~~~~~~~~~~~~~~~~~~~~*~~~~*~~~~~~~~~~~~~~~~~~~~~~~~~~*~~~~~~~~~~~~~~~~~~~~~~
    *     *                 *      *                        *
           *               *        *                      *
100선 --------------------*----------*--------------------*-----------------------
            *           *             *                 *
             * * * * * *               * * * * * * * * * 
             (하락바닥)
```

(3) 2단계 (정밀 시각화): 파이썬 렌더링 또는 외부 실제 차트 첨부
- 사용자가 더 자세한 차트를 요구할 경우, 아래 파이썬 코드를 실행하여 가상의 차트를 렌더링하거나 유사한 외부 실제 차트를 검색하여 제공한다.
- 차트 렌더링 구체 세팅 값:
  - 캔들 타입: candle 고정
  - 봉 개수: 노트에 지정이 없으므로 default(80봉) 적용
  - 거래량 표시 여부: '1분에 선긋고 거래량이 매수한다' 및 '거래량보고 매수' 규칙에 따라 default(True) 적용
  - 이평선 기간(mav): 노트에 등장하는 20, 100, 120, 200, 400선 중 문맥에 맞는 이평선 활성화 (기본 100선, 120선)
  - 마커: N형 2번째 돌파 시점, 400선 스마일 돌파 시점에 '매수(Buy)' 라벨 표기
- 파이썬 렌더링 예시 코드 (matplotlib 활용):
```python
import matplotlib.pyplot as plt
import numpy as np
import pandas as pd

# 가상의 데이터 생성 (N형 패턴: 하락 -> 1차 상승(돌파) -> 눌림 -> 2차 상승(돌파))
np.random.seed(42)
days = 80
close_prices = np.linspace(100, 50, 20).tolist() + \
               np.linspace(50, 130, 20).tolist() + \
               np.linspace(130, 90, 20).tolist() + \
               np.linspace(90, 160, 20).tolist()

df = pd.DataFrame({'Close': close_prices})
df['100MA'] = 95 # 고정된 100선 예시
df['120MA'] = 110 # 고정된 120선 예시

plt.figure(figsize=(10, 6))
plt.plot(df.index, df['Close'], label='Price (Candle trend)', color='black', linewidth=2)
plt.plot(df.index, df['100MA'], label='100MA (100선)', color='blue', linestyle='--')
plt.plot(df.index, df['120MA'], label='120MA (120선)', color='red', linestyle=':')

# 매수 타점 라벨링 (2번째 돌파 시점)
entry_x = 68
entry_y = df['Close'].iloc[entry_x]
plt.scatter(entry_x, entry_y, color='red', marker='^', s=100)
plt.annotate('매수 (N형 2번째 돌파)', xy=(entry_x, entry_y), xytext=(entry_x-10, entry_y+20),
             arrowprops=dict(facecolor='black', arrowstyle='->'))

plt.title('N형: 1차 양봉 120선 돌파 후 하락, 2번째 돌파 매수')
plt.legend()
plt.grid(True, alpha=0.3)
plt.show()
```
- 외부 차트 검색 시 키워드 템플릿:
  - "주식 100일선 N자형 반등 차트"
  - "이동평균선 스마일 패턴 돌파 매수"
  - "볼린저밴드 수축 쌍바닥 매매 기법 차트"

## 3. 추출된 용어 사전 (JSON 포맷)