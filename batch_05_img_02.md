## 1. 본문 (차트 분석 및 매매 기법)

### 3분봉 매매 기법 (400선 스마일)
- 전일 장대양봉이 나온 종목이 대상
- 당일날 얕은 우상향 400선에 이격이 크게 벌어지고 하락하면 (사용시 좋다)
- 장초에 100선, 200선에 매수간다 (1차 매수)
- 그리고 400선 내려와서 스마일 형태를 받는 위치면 2차, 3차 매수 간다
- * 고점 돌파 할때도 3차 매수 간다
- 상승 하락 반복하며 점진적 우상향 박스 형태를 띔
- ※ 100선 이탈하며 밑으로 돌아갈때 매수 (다가오지 않아 매수 못할수도 있다)

### 3분봉 횡보 및 볼린저밴드 수축 매매
- 1차 장대양봉이 나오고 횡보할때
- 볼린져밴드가 수축되고 100선위에 20선이 우상향될때 접근
- 수축 - 거래량이 없다.
- 하락하는 구간에서는 진입불가
- 지지를 딛고 안착하는 형태를 확인
- 볼린져 우상향시 중앙선 지지 받고 올라갈때 매수

## 2. AI 답변 및 단계별 시각화 가이드 (Response & Rendering Guide)

(1) 기본 답변 방식
- 답변 순서 템플릿:
  - (a) 시장상태/추세 요약 (예: 전일 장대양봉 후 얕은 우상향 하락장 등)
  - (b) 핵심 근거 (100선, 200선, 400선, 볼린져밴드 수축, 거래량 등)
  - (c) 진입 타점 (1차: 100선/200선 부근, 2/3차: 400선 스마일 반등, 볼린져 중앙선 지지)
  - (d) 무효화 조건 (100선 이탈 후 회복 실패 등)
  - (e) 손절 (노트에 명시적 손절가 부재로 리스크 관리 강조)
  - (f) 1차/2차 익절 (고점 돌파 시 추가 대응)
  - (g) 리스크/주의사항 (타점이 다가오지 않아 매수 못할 수 있음, 하락 구간 진입불가)
- 표현 원칙:
  - 노트의 표현(스마일 형태, 안착, 점진적 우상향 박스 등)을 그대로 사용한다.
  - 노트에 없는 보조지표(RSI, MACD 등)는 언급하지 않는다.

(2) 1단계 (빠른 시각화): ASCII 그래프 규칙
- 기호 매핑: 
  - 캔들 흐름: `~` (횡보), `\` (하락), `/` (상승)
  - 400선: `=====` (가장 밑의 큰 스마일 지지선)
  - 100선/20선: `-----` (위쪽 이평선 및 중앙선)
  - 타점 마커: `[1차 매수]`, `[2차/3차 매수]`, `[돌파 3차]`
- 예시 ASCII 그래프 (400선 스마일 패턴):
```text
(전고점 돌파)             /-------> [돌파 3차 매수]
                       / 
                     /
(100선) ---------\  /
    [1차 매수]    \/ (지지를 딛고 안착)
                 /
(400선) ======\_/================== (스마일 형태)
            [2/3차 매수]
```

(3) 2단계 (정밀 시각화): 파이썬 렌더링 또는 외부 실제 차트 첨부
- 사용자의 정밀 시각화 요청 시 아래 규칙에 따라 파이썬 차트를 그리거나 웹 검색 차트를 보조로 첨부한다.
- A) 파이썬 렌더링 세팅 규칙:
  - 캔들 타입: candle (default)
  - 봉 개수: 80봉 내외 (default)
  - 거래량 표시 여부: False (default, 단 볼린저 수축 시 '거래량이 없다' 라벨링)
  - 이평선 기간(mav): 20, 100, 200, 400
  - 차트 제목: "3분봉 - 400선 스마일 및 볼린져 수축 안착"
- B) 웹 검색 키워드 템플릿 (해당 시 1개만 첨부):
  - "주식 3분봉 400일선 지지 반등 차트"
  - "볼린저밴드 수축 후 중심선 지지 매매"

- 파이썬 렌더링 실행 코드 (예시 데이터 기반):
```python
import matplotlib.pyplot as plt
import numpy as np

# 가상의 시계열 데이터 생성 (80봉 기준)
x = np.arange(80)

# 400선: 얕은 우상향 유지
ema400 = 100 + x * 0.05

# 100선/200선: 하락 후 반등 (스마일 패턴 유도)
# 40선 부근에서 400선과 가까워짐
ema100 = 110 - 0.5 * x + 0.01 * (x ** 2)
ema200 = 105 - 0.2 * x + 0.005 * (x ** 2)

# 주가 흐름 (이평선 근처에서 반등하는 패턴)
price = ema100 + np.sin(x * 0.2) * 2

plt.figure(figsize=(10, 6))
plt.plot(x, price, label='Price (Candle Trend)', color='black', linewidth=1.5)
plt.plot(x, ema100, label='100선', color='blue', linestyle='-.')
plt.plot(x, ema200, label='200선', color='green', linestyle=':')
plt.plot(x, ema400, label='400선', color='purple', linewidth=2)

# 진입 마커 표기
plt.annotate('1차 매수 (100/200선)', xy=(20, ema100[20]), xytext=(10, ema100[20]+5),
             arrowprops=dict(facecolor='red', arrowstyle='->'))

plt.annotate('2차, 3차 매수 (400선 스마일)', xy=(35, ema400[35]), xytext=(25, ema400[35]-5),
             arrowprops=dict(facecolor='red', arrowstyle='->'))

plt.annotate('돌파 3차 매수', xy=(65, price[65]), xytext=(65, price[65]+5),
             arrowprops=dict(facecolor='red', arrowstyle='->'))

plt.title("3-Minute Chart: 400 EMA Smile Pattern & Support")
plt.legend()
plt.grid(True, alpha=0.3)
plt.show()
```

## 3. 추출된 용어 사전 (JSON 포맷)